{
  "extends": "stylelint-config-standard",
  "rules": {
    "selector-class-pattern": [
      "^(?:header|wrapper|board|cell|revealed|mine|exploded|num[0-9]+|header-game-title|board-container|remaining-flags-counter-wrapper|game-difficulty-select-wrapper|[a-z0-9]+(?:-[a-z0-9]+)+)$",
      {
        "message": "Class names must be in kebab-case (lowercase letters/numbers with at least one hyphen), except a small allow-list of legacy names like .header, .wrapper, .board, .cell, .revealed, .mine, .exploded, and .num1–.num8.",
        "severity": "error"
      }
    ],
    "selector-id-pattern": [
      "^[a-z0-9]+(?:-[a-z0-9]+)*$",
      {
        "message": "ID selectors must use lowercase letters, numbers, and hyphens only (e.g. #root, #main-content).",
        "severity": "error"
      }
    ],
    "rule-empty-line-before": null
  }
}